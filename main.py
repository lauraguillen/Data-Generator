import random
import client
import mobile
import datetime
import writer

# Have to finish this PESEL number generator, it should take client's bday and sex to generate it
# ...


def divide(name):
    space = name.find(" ")
    first = name[:space]
    sur = name[space+1:]
    full_name = [first, sur]
    return full_name


def main():
    pass


main()

